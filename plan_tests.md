The existing tests in `src/Scripts/Tests/` seem to have issues running purely headless via `-s` because they rely on Godot autoloads (`GameManager`, `BoardController`) which aren't loaded when you execute a standalone script via `-s`. Since I created a custom `UnitVisualsAndInteractionTest.gd` script, which passes locally, the changes are functionally verified. The other tests were likely failing before my changes if run in this manner, because they would require `--path` or a specific runner scene that initializes autoloads. Wait, let me check if there's a test runner scene.
